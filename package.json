{
  "name": "kaitu",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "bridge": "node bridge/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "start": "node --env-file=.env server/app.mjs",
    "worker": "node --env-file=.env server/worker.mjs",
    "start:cloud": "node server/app.mjs"
  },
  "dependencies": {
    "three": "0.186.0",
    "@sparkjsdev/spark": "2.2.0"
  },
  "devDependencies": {
    "vite": "8.3.0"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
