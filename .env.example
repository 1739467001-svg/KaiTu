# Generate a private .env with node scripts/init-env.mjs; never commit it.
KAITU_API_TOKEN=
KAITU_DOMAIN=your-domain.example
MAX_UPLOAD_MB=512

# Optional OpenAI-compatible Chat Completions provider. Keys stay on server.
AI_BASE_URL=
AI_MODEL=
AI_API_KEY=
